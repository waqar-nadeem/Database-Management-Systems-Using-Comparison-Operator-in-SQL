# Database-Management-Systems-Using-Comparison-Operator-in-SQL
A simple SQL program demonstrating the use of comparison operators in SELECT statements. It covers operators such as >, &lt;, >=, &lt;=, =, !=, BETWEEN, and &lt;> to filter employee records based on salary and department, highlighting basic data retrieval and conditional querying concepts.
